---
title: Radio
description: Hand-drawn radio buttons with a filled inner circle when selected.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { RadioGroup } from "@/registry/new-york/ui/radio";

<PreviewContainer
  componentName="radio"
  code={`import { RadioGroup } from '@/components/crumble/ui/radio'

<RadioGroup
  name="plan"
  defaultValue="monthly"
  options={[
    { value: 'monthly',  label: 'Monthly' },
    { value: 'yearly',   label: 'Yearly' },
    { value: 'lifetime', label: 'Lifetime' },
  ]}
/>`}
>
  <RadioGroup
    name="plan-preview"
    defaultValue="monthly"
    options={[
      { value: "monthly",  label: "Monthly" },
      { value: "yearly",   label: "Yearly" },
      { value: "lifetime", label: "Lifetime" },
    ]}
  />
</PreviewContainer>

## Installation

```bash
npx shadcn add https://crumble.dev/r/radio.json
```

## Horizontal layout

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="size"
  orientation="horizontal"
  defaultValue="md"
  options={[
    { value: 'sm', label: 'S' },
    { value: 'md', label: 'M' },
    { value: 'lg', label: 'L' },
    { value: 'xl', label: 'XL' },
  ]}
/>`}
>
  <RadioGroup
    name="size-preview"
    orientation="horizontal"
    defaultValue="md"
    options={[
      { value: "sm", label: "S" },
      { value: "md", label: "M" },
      { value: "lg", label: "L" },
      { value: "xl", label: "XL" },
    ]}
  />
</PreviewContainer>

## With disabled option

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="tier"
  defaultValue="free"
  options={[
    { value: 'free', label: 'Free' },
    { value: 'pro',  label: 'Pro' },
    { value: 'enterprise', label: 'Enterprise', disabled: true },
  ]}
/>`}
>
  <RadioGroup
    name="tier-preview"
    defaultValue="free"
    options={[
      { value: "free", label: "Free" },
      { value: "pro", label: "Pro" },
      { value: "enterprise", label: "Enterprise", disabled: true },
    ]}
  />
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="theme-demo"
  theme="crayon"
  defaultValue="b"
  options={[
    { value: 'a', label: 'Option A' },
    { value: 'b', label: 'Option B' },
  ]}
/>`}
>
  <RadioGroup
    name="theme-demo"
    theme="crayon"
    defaultValue="b"
    options={[
      { value: "a", label: "Option A" },
      { value: "b", label: "Option B" },
    ]}
  />
</PreviewContainer>
